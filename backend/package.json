{
  "name": "ga-enterprise-api",
  "version": "1.0.0",
  "description": "GA Enterprise System - Cloudflare Workers API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.104.0"
  }
}
